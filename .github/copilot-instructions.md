# Copilot Instructions

## Project Guidelines
- For CreepingBorders mod deployment: Run deploy.bat instead of manually copying DLL files. This script handles the deployment process automatically.
- Do not create markdown documents unless requested